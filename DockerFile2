FROM huggingface/transformers-pytorch-gpu
RUN apt-get update && apt-get install -y wget git python3.8 python3.9 curl

RUN git clone https://github.com/hkjeon13/koai.git
WORKDIR koai/
RUN git fetch --all && git pull origin main

#RUN python3.8 -m pip install setuptools patchelf &&  \
#    python3.9 -m pip install setuptools patchelf && \
#    curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh && \
#    python3.8 -m pip install -r requirements.txt &&  \
#    python3.9 -m pip install -r requirements.txt
#RUN python3.8 setup.py bdist_wheel && python3.9 setup.py bdist_wheel

